import Joi from 'joi'
import _ from 'lodash'
import { visitTypes } from '../interfaces/visit'

export const id = Joi.string().trim().pattern(/^[a-fA-F0-9]{24}$/, "str")
export const optionId = Joi.string().trim().allow("").pattern(/^[a-fA-F0-9]{24}$/, "str")
export const limit = Joi.number().integer().min(1).max(100)
export const categoryLimit = Joi.number().integer().min(1).max(1000)
export const offset = Joi.number().integer().min(0)
export const searchWord = Joi.string().trim().min(0).max(20)
export const userName = Joi.string().trim().pattern(/^([a-zA-Z]|[가-힣]|[0-9])+$/, "str").min(3).max(20) 
export const summary = Joi.string().trim().min(3).max(100)
export const title = Joi.string().trim().min(3).max(200)
export const text = Joi.string().trim().min(3).max(10000)
export const order = Joi.number().integer().min(1)
export const idArray = Joi.array().items(_.cloneDeep(id))
export const stringArray = Joi.array().items(Joi.string().trim().min(1).max(100))
export const email = Joi.string().email()
export const password = Joi.string().length(64)
export const settingType = Joi.string().trim().pattern(/^[a-fA-F0-9]{24}$/, "str")
export const settingName = Joi.string().max(64).min(1)
export const settingValue = Joi.string().max(64).min(1)
export const role = Joi.string().trim().pattern(/^[a-fA-F0-9]{24}$/, "str")
export const category = Joi.string().min(1).max(40)
export const categories = Joi.array().items(_.cloneDeep(category)).min(1).max(1000)
export const visitTarget = Joi.string().trim().min(4).max(24)
export const visitType = Joi.string().valid(...visitTypes).trim().min(1).max(20)
